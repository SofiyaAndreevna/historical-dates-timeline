export const ANIMATION_CONFIG = {
  yearAnimation: {
    duration: 1,
    ease: 'power2.inOut',
  },

  circleRotation: {
    duration: 0.8,
    ease: 'power2.inOut',
  },

  defaults: {
    ease: 'power2.inOut',
  },
} as const;

